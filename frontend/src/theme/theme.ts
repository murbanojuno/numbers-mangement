import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#298899',
    },
    secondary: {
      main: '#E90018',
    },
    background: {
      default: '#FFFFFF',
    },
    text: {
      primary: '#333',
    },
    error: {
      main: '#E90018',
    }
  },
});
